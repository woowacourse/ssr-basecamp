// 🚨 member.js 파일은 본 미션을 진행하기 위한 참고용 코드입니다.
// 사전 미션에서는 사용하지 않습니다.

// import { Router } from "express";
// import fetch from "node-fetch";

// const router = Router();

// /* 자세한 정보는 API 문서 참고
//     https://expressjs.com/ko/4x/api.html#router
// */
// router.get("/", async (_, res) => {
//   // ex: /members -> 모든 회원 정보를 가져옴
//   const members = await fetch("https://jsonplaceholder.typicode.com/users");

//   res.json(await members.json());
// });

// router.get("/:id", async (req, res) => {
//   // ex: /members/3 -> 3번 회원에 해당하는 회원 정보를 가져옴
//   const member = await fetch(`https://jsonplaceholder.typicode.com/users/${req.params.id}`);

//   res.json(await member.json());
// });

// export default router;
